---
layout: layout.njk
title: Blog
---

<h2 class="text-3xl font-bold mb-8 text-[#6089FB]">Weekly Blog Posts</h2>
<ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
  {% for post in collections.posts | reverse %}
    <li class="bg-white shadow rounded-lg p-4 hover:shadow-md transition">
      <h3 class="text-xl font-semibold mb-2 text-[#6089FB]">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
      </h3>
      <p class="text-sm text-gray-500 mb-4">{{ post.date | date("MMMM D, YYYY") }}</p>
      <p>{{ post.templateContent | striptags | truncate(120, true, '...') }}</p>
    </li>
  {% endfor %}
</ul>
